#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
#define endl '\n'

using namespace std;

void

int LCS(string& a, string& b) {
	buildSuffixArray(a, b);
}

int main() {
	ios::sync_with_stdio(false);
	cin.tie(0);

	cout << flush;
}